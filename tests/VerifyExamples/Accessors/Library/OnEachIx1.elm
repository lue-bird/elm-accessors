module VerifyExamples.Accessors.Library.OnEachIx1 exposing (..)

-- This file got generated by [elm-verify-examples](https://github.com/stoeffel/elm-verify-examples).
-- Please don't modify this file by hand!

import Test
import Expect

import Accessors.Library exposing (..)
import Lens as L
import Accessors.Library exposing (..)
import Accessors exposing (..)



multiplyIfGTOne : (Int, { bar : Int }) -> (Int, { bar : Int })
multiplyIfGTOne ( idx, ({ bar } as rec) ) =
    if idx > 0 then
        ( idx, { bar = bar * 10 } )
    else
        (idx, rec)
listRecord : {foo : List {bar : Int}}
listRecord = { foo = [ {bar = 2}
                     , {bar = 3}
                     , {bar = 4}
                     ]
             }



spec1 : Test.Test
spec1 =
    Test.test "#onEachIx: \n\n    get (L.foo << onEachIx << snd << L.bar) listRecord\n    --> [2, 3, 4]" <|
        \() ->
            Expect.equal
                (
                get (L.foo << onEachIx << snd << L.bar) listRecord
                )
                (
                [2, 3, 4]
                )